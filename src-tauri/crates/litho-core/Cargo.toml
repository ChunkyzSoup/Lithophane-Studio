[package]
name = "litho-core"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["bmp", "jpeg", "png", "webp"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
